# Alembic Configuration — placeholder
#
# CrimeMill uses Supabase SQL Editor for migrations, not Alembic.
# This file exists to document that decision. All schema changes
# live in supabase/migrations/ and are applied via the Supabase dashboard
# or `supabase db push`.
#
# If we migrate away from Supabase in the future, this file will
# be configured for standard Alembic usage.
